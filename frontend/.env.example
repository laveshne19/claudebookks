# Nalanda ERP frontend environment.
# Empty = same-origin: the app calls "/api" and the reverse proxy (nginx)
# forwards to the backend. Set a full URL only for a split-domain setup,
# e.g. REACT_APP_BACKEND_URL=https://api.yourdomain.com
REACT_APP_BACKEND_URL=
