enable-pre-post-scripts=false
# Don't re-run an install check before `pnpm run build` (avoids pnpm-11
# aborting the build over unapproved native build scripts that vite doesn't need).
verify-deps-before-run=false
