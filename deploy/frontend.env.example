# Copy to frontend/.env and fill in. Used at BUILD time (CRA/craco).
# Use your domain (or http://VPS_IP) — the path /api is proxied to the backend by nginx.
REACT_APP_BACKEND_URL=http://YOUR_DOMAIN_OR_IP
